export type ListUsuarioApi = {
    id: number;
    nome: string;
    login: number;
    status: boolean;
    roles: Array<string>;
}

export type ListUsuario = {
    id: number;
    name: string;
    login: number;
    status: boolean;
    roles: Array<string>;
}